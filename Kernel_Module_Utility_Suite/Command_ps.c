#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
#include<sys/utsname.h>

//  ./psx
//  argv[0] 

//  argc = 1

/*
    Open the directory /proce //opendir open only int file and only open file 
    Open all subdirectors whose name is integer   // isdigit
    Inside that directory open comm file
    Display the name inside that file
*/

int main(int argc, char *argv[])
{ 

    return 0;
}